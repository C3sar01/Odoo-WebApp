<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="compra.list">
      <field name="name">compra list</field>
      <field name="model">compra.compra</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="compra.action_window">
      <field name="name">compra window</field>
      <field name="res_model">compra.compra</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="compra.action_server">
      <field name="name">compra server</field>
      <field name="model_id" ref="model_compra_compra"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="compra" id="compra.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="compra.menu_1" parent="compra.menu_root"/>
    <menuitem name="Menu 2" id="compra.menu_2" parent="compra.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="compra.menu_1_list" parent="compra.menu_1"
              action="compra.action_window"/>
    <menuitem name="Server to list" id="compra" parent="compra.menu_2"
              action="compra.action_server"/>
    -->
  </data>
</odoo>